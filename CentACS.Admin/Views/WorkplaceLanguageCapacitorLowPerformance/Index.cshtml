@model IEnumerable<CentACS.Admin.Data.tblWorkplace_LanguageCapacitorLowPerformance>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LanguageKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LowPerfStrategy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LanguageKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LowPerfStrategy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CapacitorLowPerfKey }) |
                @Html.ActionLink("Details", "Details", new { id=item.CapacitorLowPerfKey }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CapacitorLowPerfKey })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
