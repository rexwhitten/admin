@model IEnumerable<CentACS.Admin.Data.tblWorkplace_LanguageConsultantResponseBias>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LanguageKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResponseBias)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResponseBiasText)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LanguageKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponseBias)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponseBiasText)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.LangaugeResponseBiasKey }) |
                @Html.ActionLink("Details", "Details", new { id=item.LangaugeResponseBiasKey }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.LangaugeResponseBiasKey })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
